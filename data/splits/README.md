# Frozen Splits

Store split files as immutable ID lists with cluster IDs and a generation manifest.

Recommended filename:

aviation_cluster_safe_v1.json

If the split changes, create v2. Do not overwrite v1 after reporting results.


# ── Core (corpus building + Tier-1 CRF/LogReg + reporting) — CPU only ──────────
pandas>=2.0
numpy>=1.24
PyYAML>=6.0
scikit-learn>=1.3
sklearn-crfsuite>=0.3.6
matplotlib>=3.7

# ── Tier-2 neural entity/relation models (BiLSTM-CRF + neural RE) ──────────────
# GPU build (recommended, CUDA 12.1):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# CPU build:  pip install torch
torch>=2.0
pytorch-crf>=0.7.2

# ── Domain word embeddings for Tier-2 (scripts/08_make_embeddings.py) ─────────
gensim>=4.3

# ── Tier-3 transformer encoder (BERT NER + span-pooling RE) ───────────────────
# encoder is set in config/schema.yaml (models.transformer.encoder)
transformers>=4.40

# ── Tier-3 SpERT — use the official repo github.com/lavis-nlp/spert
#    (transformers + torch). scripts/06_export_spert.py exports gold to its format.

pytest>=7.0


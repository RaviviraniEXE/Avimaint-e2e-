# Output Contract

Each run must have an immutable run directory:

outputs/runs/<experiment_id>/<run_id>/

It should contain configuration, manifest, predictions, metrics and error analysis. Dashboard pages must read these artifacts rather than importing training packages.

